<section class="section">
  <div class="container" style="max-width: 820px;">
    <span class="eyebrow">Conteúdo</span>
    <h1>{{ page.title }}</h1>
    <div class="page-content" style="margin-top: 32px; color: var(--brand-slate-700); font-size: 16px; line-height: 1.75;">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{ "name": "Página", "settings": [] }
{% endschema %}
